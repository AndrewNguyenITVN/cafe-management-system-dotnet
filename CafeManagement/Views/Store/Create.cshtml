@model CafeManagement.Models.Domain.Store
@{
    Layout = "_AdminLayout";
    ViewData["Title"] = "Thêm Chi nhánh";
}

<div class="d-flex align-items-center mb-4">
    <a asp-action="Index" class="btn btn-outline-secondary btn-sm me-3">← Quay lại</a>
    <h4 class="fw-bold mb-0">Thêm Chi nhánh mới</h4>
</div>

<div class="card p-4" style="max-width: 600px;">
    <form asp-action="Create" method="post">
        @Html.AntiForgeryToken()
        <div asp-validation-summary="ModelOnly" class="alert alert-danger py-2"></div>

        <div class="mb-3">
            <label asp-for="Name" class="form-label fw-semibold">Tên chi nhánh <span class="text-danger">*</span></label>
            <input asp-for="Name" class="form-control" placeholder="VD: Chi nhánh Quận 1" />
            <span asp-validation-for="Name" class="text-danger small"></span>
        </div>
        <div class="mb-3">
            <label asp-for="Address" class="form-label fw-semibold">Địa chỉ</label>
            <input asp-for="Address" class="form-control" placeholder="Số nhà, đường, quận" />
        </div>
        <div class="mb-3">
            <label asp-for="Phone" class="form-label fw-semibold">Hotline</label>
            <input asp-for="Phone" class="form-control" placeholder="0901234567" />
        </div>
        <div class="row mb-3">
            <div class="col">
                <label asp-for="OpenTime" class="form-label fw-semibold">Giờ mở cửa</label>
                <input asp-for="OpenTime" type="time" class="form-control" />
            </div>
            <div class="col">
                <label asp-for="CloseTime" class="form-label fw-semibold">Giờ đóng cửa</label>
                <input asp-for="CloseTime" type="time" class="form-control" />
            </div>
        </div>

        <div class="d-flex gap-2 mt-4">
            <button type="submit" class="btn btn-primary px-4">Lưu</button>
            <a asp-action="Index" class="btn btn-outline-secondary px-4">Hủy</a>
        </div>
    </form>
</div>

@section Scripts { <partial name="_ValidationScriptsPartial" /> }
